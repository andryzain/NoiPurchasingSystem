@using NoiPurchasingSystem.Areas.MasterData.ViewModels;

@{
    ViewBag.Title = "Hapus Berat";
    Layout = "~/Views/Shared/_TopNavbarMasterData.cshtml";
}

@model BeratViewModel

<div class="card card-red">
    <div class="card-header">
        <h3 class="card-title" style="color:#ffffff">@ViewBag.Title</h3>
    </div>


    <form enctype="multipart/form-data" asp-controller="Berat" asp-action="DeleteBerat" method="post" class="form-horizontal">
        <div class="card-body">
            <h4>Yakin hapus data ini?</h4>
            <hr />
            <input hidden asp-for="BeratId" />
            <input hidden asp-for="KodeBerat" />
            <input hidden asp-for="Nilai" />
            <div class="post">
                <div class="user-block">
                    <span class="username">
                        <a href="#">@Html.DisplayFor(model => model.Nilai)</a>
                    </span>
                    <span class="description">@Html.DisplayFor(model => model.KodeBerat)</span>
                </div>
                <!-- /.user-block -->
            </div>
        </div>

        @if (TempData["WarningMessage"] != null)
        {
            <script type="text/javascript">
                window.onload = function () {
                    swal("Gagal", '@TempData["WarningMessage"]', "warning");
                };
            </script>
        }

        <div class="card-footer">
            @*<form asp-action="DeleteBerat" method="post">*@
            <a class="btn btn-default" asp-action="Index" asp-controller="Berat" asp-area="MasterData">Batal</a>
            <button type="submit" class="btn btn-danger float-left fw-bold"><div style="color:#ffffff">Hapus</div></button>
            @*</form>*@
        </div>
    </form>
</div>
